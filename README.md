# docker-add-fetch-url-demo

Static HTML demo. To run a local dev server:

```
npm run dev
```

If `py` is not available on your system, use:

```
npm run dev:python
```

Then open http://localhost:3000/index.html (avoid http://[::]:3000).
